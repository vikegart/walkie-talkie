{
  "name": "walkie-talkie",
  "version": "0.0.1",
  "description": "Keep in touch with your favrouite ones",
  "scripts": {
    "deploy": "npm i && node server/index.js",
    "start": "cd server && node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.16.3",
    "socket.io": "4.5.3"
  }
}
