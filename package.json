{
  "name": "walkie-talkie",
  "version": "0.0.1",
  "description": "Keep in touch with your favrouite ones",
  "scripts": {
    "deploy": "npm i && cd server && node index.js",
    "start": "cd server && node index.js"
  },
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.1"
  }
}
